
void DFS(ProbInfo **prob, int *maxEnergy, Cel_List currentPath, Cel_List *bestPath) {
    
    (*prob)->max_energy = (*prob)->inicial_energy;
    // Se chegámos ao fim de um dos caminhos
    if ((*prob)->k == 0) {
        
        // Se o current caminho for melhor, atualizar
        if () {

        }
        return;
    }

    
    
    if (/*se poderemos ir para esta célula*/ ) {
        

        // Adicionar a célula

        // Recursão
        DFS(prob, (*prob)->k);

        // Backtracking
        free(newNode);
    }
}

